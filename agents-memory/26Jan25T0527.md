Summary: Setup GitHub Pages with webcc example.

# User Prompt
add index.html to be the github page for this repo

copy webcc example dist directory to under a child folder under dist folder at root

have the root index.html point to the webcc example

also follow agents.md

# Agent Response
I will setup the GitHub pages structure by creating a `dist` directory, copying the webcc example, and creating a root `index.html`.
