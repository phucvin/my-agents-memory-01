Summary: Updated build artifacts script for MoonBit examples to include JS and LLVM outputs.

# User Prompt
update moonbit examples build artifacts script to build with '--target all' and '--output-wat' to copy js and llvm output files as well

mention the build script in readme

also follow agents.md

# Agent Response
Updated `codes/moonbit_examples/scripts/build_artifacts.py` to:
1. Build all targets (`native`, `wasm`, `wasm-gc`, `js`) using `moon build --target all --output-wat`.
2. Copy `.js` files from `target/js/release/build/cmd/{package}/{package}.js`.
3. Copy `.ll` files from `target/native/release/build/cmd/{package}/{package}.ll`.

Updated `codes/moonbit_examples/README.md` to:
1. Mention `scripts/build_artifacts.py`.
2. List `.js` and `.ll` in the Generated Artifacts section.

Followed `AGENTS.md` by logging this session.
