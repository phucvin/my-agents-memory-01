Summary: Prototype a regex VM in C++ with group extraction.

# User Prompt
prototype a regex vm that can parse regex, check match and extract matched groups

use C++ and multiple code files instead of just 1 file

add tests and readme

also follow agents.md

# Agent Response
I will implement a Pike VM based regex engine in C++. This approach allows for efficient matching and submatch extraction.
