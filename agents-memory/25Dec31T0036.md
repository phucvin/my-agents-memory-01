Summary: New session for creating the 'silang' language.

# User Prompt
write silang, an interpreted statically typed language with C/JS-like syntax.

write the host in C++. silang's standard libraries can be implemented in the host.

a goal for silang is also to write silang interpreter in silang.

no need to free memory during execution of a silang module.

also follow agents.md

# Agent Response
I will start by creating the `codes/silang` directory and defining a plan to implement the language, including the lexer, parser, interpreter, and standard library support in C++, with a goal of enabling self-hosting.
