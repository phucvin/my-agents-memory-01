Summary: Updated AGENTS.md to include instructions for agents to scan the `agents-memory` directory for past information.

# User Prompt

update agents.md so agent would scan agents-memory directory to look for useful information in the past, when it needs to

# Agent Response

I have updated `AGENTS.md` to include instructions for agents to scan the `agents-memory` directory when looking for useful information in the past.

The new section added is:

```markdown
## Memory Retrieval

When you need to look for useful information in the past, scan the `agents-memory` directory.
```
