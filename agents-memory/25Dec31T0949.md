Summary: Adding 10 more JS examples to `codes/static_hermes_examples`, compiling them to C, removing `install_moon.sh`, and investigating `shermes` build status.

# User Prompt
add 10 more js examples to hermes, varying js features, and compile to keep their C

remove install moon

also follow agents.md

# Agent Response
I will add 10 JavaScript examples to `codes/static_hermes_examples/`, varying the features used.
I will remove `install_moon.sh`.
I will ensure `AGENTS.md` protocols are followed, including logging this session and updating the README.
I am currently investigating the availability of the `shermes` compiler to generate the C files.
