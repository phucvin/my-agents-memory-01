Summary: Compilation of 10 JS examples to Wasm GC using Static Hermes.

# User Prompt
compile 10 js examples to webassembly (use WAT format) using Wasm GC
the host of running those compiled wasm provide only simple IO functions (e.g. putchar, getchar)
put the js and wat files into codes/llm_compiler_01

# Agent Response
I will creating the JS examples first. Then I will install `wabt` and build `Static Hermes` (branch `shermes-wasm`) to compile them to WAT.
